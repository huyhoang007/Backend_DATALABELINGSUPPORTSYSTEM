server:
  port: 8080

spring:
  datasource:
    url: jdbc:postgresql://dpg-d6np6oshg0os73c93r50-a.singapore-postgres.render.com:5432/datalabeling
    username: datalabeling_user
    password: gHxMAP0qunUnPAbwceIaGQUQPgCVr3uE
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect

  servlet:
    multipart:
      enabled: true
      max-file-size: 10MB
      max-request-size: 100MB

  # Serve static files từ thư mục uploads
  web:
    resources:
      static-locations: classpath:/static/,file:uploads/

logging:
  level:
    com.datalabeling.datalabelingsupportsystem: DEBUG
    org.springframework.aop: DEBUG
    org.aspectj: DEBUG

jwt:
  secret: ibelievewecandoitmyguysneverstopgoingweneedtogoforeverlifeisanunstoppablejourneyofobstaclesinwhichwecannotavoid
  expiration: 86400000

app:
  upload:
    path: uploads